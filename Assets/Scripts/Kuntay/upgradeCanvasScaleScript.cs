using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class upgradeCanvasScaleScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // canvasın canvas scalerdaki scale degeri ekranın yüksekliği ile doğru orantı ile değişecek.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
